# MCP Demo

This directory represents MCP assets owned by the Deep Agents project.

The GEPA example does not optimize this server implementation. It only carries
the MCP server config into runtime setup and optimizes declared MCP tool
descriptions such as `mcp:tool:search_routing_risks:description`.
